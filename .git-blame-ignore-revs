# Revisions ignored by `git blame` — bulk, no-behaviour-change reformatting only.
# Enable locally: git config blame.ignoreRevsFile .git-blame-ignore-revs
# Entries are appended by the style-commit bot; one SHA per line, with a comment.
# Everything above this line is a seed; everything below is this repository's history.

# style: apply PHP-CS-Fixer to src/ and tests/
b975d6b5e0a44ce24233113d19eac0c5d60fa5f8

# style: one space around the concatenation operator (concat_space override)
669a6e3892d2e8aae84c1528db3fdfa52e53b9a1
